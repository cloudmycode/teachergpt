# 复制本文件为 config.toml 并填入真实 key（config.toml 已被 .gitignore 忽略，不会提交）
#   cp script/config.example.toml script/config.toml
# 也可用环境变量 DEEPSEEK_API_KEY 覆盖，优先级高于本文件。

[deepseek]
api_key = "sk-填入你的DeepSeek密钥"
base_url = "https://api.deepseek.com"
model = "deepseek-chat"

[coze]
api_key = "pat_填入你的Coze密钥"
