# 2026-06-14 工作日志

## build_timeline.py 按最新 TTS 方案重写

- 移除所有 edge-tts 代码（`_synth_with_timings`、`_match_keyword_timings`、WordBoundary 相关）
- 移除三级 fallback 时长读取（`_read_audio_duration`、`_estimate_mp3_duration`、`_fallback_keyword_timings`）
- TTS 合成：CosyVoice 2.0（`cosyvoice/models/CosyVoice2-0.5B`），输出 wav
- 时长读取：mutagen（简单可靠）
- 关键词时间戳：WhisperX 强制对齐（`align_keywords()`），无降级
- `--skip-align` 替代旧 `--skip-tts` 的部分功能，允许跳过对齐保留 narration
- `--skip-generate` 已删除：JSON 存在时自动跳过生成，无需标志
- design.md 11.5 扩展为 `build_timeline.py` 详细描述（流程、用法、参数、缓存行为）
- design.md 新增 11.7 部署环境要求（Python 3.10+/系统包/模型/部署清单）
- requirements.txt 去掉 cosyvoice，改为注释说明源码安装方式
- generate.py `_extract_lesson` 加入诫子书等常见文言课文
- CosyVoice2 无 `device` 参数（auto-detect），三处文件已修正
- CosyVoice 需 Python 3.10+、Matcha-TTS submodule 需单独 pip install

## CosyVoice 安装方式修正

- PyPI 上的 `cosyvoice` 是残缺包（缺 `cli/` 目录），已卸载
- 正确方式：从 GitHub 源码安装 `git clone --recursive https://github.com/FunAudioLLM/CosyVoice.git`
- 通过 `COSYVOICE_HOME` 环境变量指向源码目录，自动加到 sys.path
- 更新了 `build_timeline.py`、`cosyvoice/synthesize.py`、`cosyvoice/README.md`、`docs/design.md` 的安装说明
