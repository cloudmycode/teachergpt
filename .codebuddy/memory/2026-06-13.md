# 2026-06-13 工作日志

## design.md 更新

### 11.4.4 PPT 渲染方案重写
- 删除"HTML → 截图 → PPT"方案
- 改为 python-pptx 原生生成可编辑 PPT
- 添加详细代码示例和高亮实现要点
- 说明优缺点

### 11.4.4 PPT → 视频
- 添加 PPT 导出视频的三种方案对比
- 有 Office：powerpnt /video 命令行
- 无 Office：PPT → HTML → Playwright 录屏
- 服务器轻量：保持图片+ffmpeg 方案
- 推荐复用 PPT 生成视频，避免两套流程

### 11.4.2.1 关键词高亮时间方案改进
- 原方案"位置比例推算"评估为次优（语速非恒定，误差1~2秒）
- 改为：主用 edge-tts WordBoundary 事件获取精确词边界时间戳
- 位置比例推算降为 fallback（TTS 不支持时间戳时）
- edge-tts 关键技术点：Communicate.stream() 产生 WordBoundary chunk，offset/duration 单位为 100ns
- 同步更新了 11.4.2 / 11.5.1 / 11.6 三处引用描述

## 11.4.2.1 再次修订：TTS 方案敲定为 CosyVoice + WhisperX

- 实测确认 edge-tts **中文音色不返回 WordBoundary**（服务端只回 SentenceBoundary），11.4.2.1 原"主用 WordBoundary"说法对中文不成立
- 改为 **CosyVoice 2.0 出音 + WhisperX 强制对齐** 作为唯一方案，不保留降级路径
- features.md 中所有 edge-tts WordBoundary/fallback 引用统一替换为 CosyVoice + WhisperX
- requirements.txt 移除 edge-tts、mutagen，新增 cosyvoice、whisperx

## cosyvoice/ 目录创建

- 参照 bge/ 命名规范（download_model.py + 使用脚本）创建 `cosyvoice/`：
  - `download_model.py`：检查/下载 CosyVoice2-0.5B 到 `cosyvoice/models/`，支持 --download --force --test
  - `synthesize.py`：文本 → mp3，支持情感标签（<happy>/<sad>/<calm> 等），本地离线推理，--model 指向本地模型目录
  - `README.md`：使用文档，格式对齐 bge/README.md

## 第 11 章重写

- 合并"路线 A（PPT/网页）"和"路线 B（视频）"为一个统一章节：
  - 一条管线（生成 → TTS → 对齐 → timeline.json），三种输出（HTML / PPT / MP4）
  - 11.3 核心管线：按句生成 → CosyVoice 合成 → WhisperX 对齐 → timeline.json
  - 11.4 三种渲染：HTML（浏览器直开）、PPT（python-pptx）、MP4（PIL + ffmpeg）
  - 删掉了 11.4/11.5 两条路线的重复描述，合并到同一节
