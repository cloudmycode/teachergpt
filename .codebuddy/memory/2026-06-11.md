# 2026-06-11

## script/transcribe_dir.py bug 修复
检查并修复 3 个 bug：
1. `process_one` 占位 `touch` 前未建父目录 → 子目录 mp3 抛 FileNotFoundError 崩溃。已加 `out_dir.mkdir(parents=True, exist_ok=True)`。
2. 中断遗留的 0 字节占位 txt 会被 `process_one` 的 `touch(exist_ok=False)` 当成 FileExistsError → 永久 skip。已改为：非空才 skip，0 字节复用重处理。
3. `build_tasks` 按 `txt.stem` 去重，跨子目录同名文件互相误判跳过。已改为按实际目标路径 `out_txt` 判断完成。

lint 剩余的都是 basedpyright 严格模式类型噪音，非真实 bug。

## 占位机制修正（接上）
之前第 2 条"0 字节复用重处理"破坏了多进程占位（A 刚占位未写完，B 见 0 字节也去处理 → 重复）。已分两阶段解决：
- 启动阶段 `build_tasks`（单进程）：0 字节遗留占位直接 unlink 重入队，安全。
- 运行阶段 `process_one`（多进程）：目标已存在即 skip，否则 `touch(exist_ok=False)` 原子抢占。
约束：仅支持「单脚本 --workers N」；手动并发多个脚本实例时启动清理会误删他人占位，需改 mtime 陈旧判断/锁文件。

## 多进程"开始"日志
之前绕弯想让主进程拿开始事件（imap/apply_async 都只有结果回调，无开始回调）。正解：在 process_one 子进程里直接 `print(..., flush=True)` 打印开始行。结束行仍由主进程 on_done 打印，二者交错靠 pid 区分。

## script/clean_transcript.py（纠错+断句合并 pipeline）
新建。输入 data/chinese_text/<课>/<NNN-标题>.txt（行格式 `start s → end s | 文本`），输出 data/chinese_clean/ 下同结构 .jsonl（字段 lesson/segment_id/t_start/t_end/text）。
- 纠错+合并一次 DeepSeek 调用完成（OpenAI 兼容 /chat/completions，urllib 零依赖，model deepseek-chat）。
- 核心「序号锚定法」：送模型「行号+纯文本」（不含时间戳），模型输出 `[a-b] 段文字`，时间戳由代码按行号区间从原始行还原，模型不碰时间戳。每块校验区间连续/不重叠/全覆盖，失败重试 MAX_RETRY 次，仍失败降级为原始行不合并（不静默吞行）。
- 分块 CHUNK_LINES=120，向后看 20 行在 gap>=0.8s 自然停顿处切；输出先写 .tmp 再原子 replace；按目标存在且非空跳过（--force 强制、--dry-run 只检查解析分块、--file 单文件）。
- 密钥：script/config.toml（已 gitignore）的 [deepseek].api_key，或环境变量 DEEPSEEK_API_KEY 覆盖；提交 script/config.example.toml 模板。tomllib 在 <3.11 解释器回退到极简 key=value 解析。
- dry-run 实测 24 文件、每文件 399~523 行切 3~4 块正常。当前机器无 python3.13（系统 python3 是 3.9），实际跑需用 3.13。
