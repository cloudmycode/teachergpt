# 2026-06-17

## script/buildclass.py（合并脚本）
- 把 build_timeline.py + build_pptx.py 合并成单一入口 buildclass.py，并删除这两个旧文件。
- 流程：script.json/timeline.json 必选；`--audio` 才合成 Coze TTS 音频（默认不生成，与旧 build_timeline 默认开 TTS 相反）；`--pptx` 出 PPT；`--mp4` 仅打印 PowerPoint 手动导出提示（macOS 无可靠自动转换）。
- 用法：`python3 script/buildclass.py "诫子书 全文" --audio --pptx --mp4`。
- main 里只 parse_intent 一次，传给 build_script，避免重复 LLM 调用。pptx 库延迟到 _pptx_consts()/build_pptx 内导入，纯文本流程不强依赖 python-pptx。
- server/app.py 改为单次调 buildclass（--audio --pptx），_run_buildclass 取代旧的 _run_build_timeline/_run_build_pptx，pptx 路径查找改 rglob 兼容课程/章节两级目录。build_player.py 提示串也改成 buildclass。

### 篇名/出处混淆修复
- 之前 context 引导句用 source（出处，如《归田录》）当篇名，导至 intro/narration 把出处当成标题念。
- generate.py `fetch_lesson_facts` 注入 `title` 字段（= intent["lesson"]，如"卖油翁"）。
- 新增 `_wrap_title`/`_lesson_intro_line` 辅助，context 改为"你正在讲《卖油翁》（欧阳修，北宋，选自《归田录》）"，LLM 不会再把出处当篇名。
- generate.py 编译通过。

### intro 背景 + PPT 翻页停顿
- generate.py `fetch_lesson_facts` JSON 模板加了 `background` 字段（写作背景），`_build_intro_context` 注入背景信息，`PER_INTRO_USER` 要求 intro 简要介绍作者/朝代/写作背景。
- buildclass.py `_embed_audio_post` 封面页停留时间加 1000ms（intro 读完后停 1 秒再翻页）。
- 两端均编译通过，lint 无新增问题。

## tts/coze_tts.py
- Coze TTS 单次 input 限制 1024 字符，超长需拆句。
- 已抽出 `_split_text()`：按 `。！？；`+换行切句 → 单句仍超长按字符硬切（兜底，避免无标点长句再触发 4000）→ 贪心合并到 1000 字内（MAX_INPUT_LEN，留余量）。
- 各段分别合成后直接拼接 mp3 字节（同编码可直接 concat），不依赖 ffmpeg。
- 修复 bug：`len(out_path.stat().st_size)` → `out_path.stat().st_size`（int 不能 len）。
- API Key 读取优先级：环境变量 COZE_API_KEY > script/config.toml [coze].api_key > 传参。
- build_timeline.py 的 TTS 已改用 tts.coze_tts.synthesize（替换原 edge-tts）。

## generate.py / build_timeline.py 职责重构
- 决策：不合并成单文件。让 generate.py 当唯一的"LLM/prompt 库"（无 main），build_timeline.py 退化为纯编排。
- generate.py：删除整篇生成相关的 build_prompt / save_output / parse_args / main / TIMELINES_DIR / `if __name__`（这套整篇 markdown 生成已确认弃用）；新增公共函数 build_style_system(style)、build_few_shot(segments, limit=3, maxlen=200)；并把逐句/导入语 prompt（PER_INTRO_USER/PER_SENTENCE_USER、_build_context/_build_intro_context、build_per_sentence_prompt/build_intro_prompt）从 build_timeline.py 搬进来。删了未用 import argparse/time。
- build_timeline.py：删除上述 prompt 函数（含 _build_system），改调用 generate.build_intro_prompt / generate.build_per_sentence_prompt。其余只保留分批/TTS/对齐/章节拆分/存盘/main。
- 现在"和大模型交互(call_deepseek) + 全部 prompt 构建"都集中在 generate.py 一处。
- PER_SENTENCE_USER 串讲要求（第3条）改为：先一字不差念文言原句，再用"就是说"接白话翻译，最后点妙处；带孙权劝学示例，禁止跳过原句直接讲白话。（撤掉了之前的"开头单独朗读一遍"那条，避免重复念两次）

